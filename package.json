{
  "name": "alvo-proposta",
  "version": "1.0.0",
  "private": true,
  "homepage": "https://marceliomp.github.io/Alvo-proposta",
  "scripts": {
    "start": "react-scripts start",          // ou "vite" se for Vite
    "build": "react-scripts build",          // ou "vite build"
    "postbuild": "cp build/index.html build/404.html || copy build\\index.html build\\404.html",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  }
}
